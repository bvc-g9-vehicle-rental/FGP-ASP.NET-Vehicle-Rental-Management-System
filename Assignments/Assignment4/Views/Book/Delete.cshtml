@model Assignment4.Models.Book
@{
    ViewData["Title"] = "Delete Book";
}

<div class="card border-danger" style="max-width:500px">
    <div class="card-header text-danger">🗑️ Confirm Delete</div>
    <div class="card-body">
        <p>Are you sure you want to delete this book?</p>
        <dl class="row">
            <dt class="col-sm-4 form-label">Title</dt>
            <dd class="col-sm-8"><strong>@Model.Title</strong></dd>
            <dt class="col-sm-4 form-label">Author</dt>
            <dd class="col-sm-8">@Model.Author</dd>
            <dt class="col-sm-4 form-label">ISBN</dt>
            <dd class="col-sm-8">@(Model.ISBN ?? "—")</dd>
        </dl>
        <div class="alert alert-warning small">⚠️ This action cannot be undone.</div>
    </div>
    <div class="card-footer d-flex gap-2">
        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger btn-sm">Yes, Delete</button>
        </form>
        <a asp-action="Index" class="btn btn-outline-secondary btn-sm">Cancel</a>
    </div>
</div>
