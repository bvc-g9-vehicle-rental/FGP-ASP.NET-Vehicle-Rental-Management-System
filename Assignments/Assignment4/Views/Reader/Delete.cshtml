@model Assignment4.Models.Reader
@{
    ViewData["Title"] = "Delete Reader";
}

<div class="card border-danger" style="max-width:500px">
    <div class="card-header text-danger">🗑️ Confirm Delete</div>
    <div class="card-body">
        <p>Are you sure you want to delete this reader?</p>
        <dl class="row">
            <dt class="col-sm-4 form-label">Name</dt>
            <dd class="col-sm-8"><strong>@Model.Name</strong></dd>
            <dt class="col-sm-4 form-label">Email</dt>
            <dd class="col-sm-8">@Model.Email</dd>
        </dl>
        <div class="alert alert-warning small">⚠️ This action cannot be undone.</div>
    </div>
    <div class="card-footer d-flex gap-2">
        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger btn-sm">Yes, Delete</button>
        </form>
        <a asp-action="Index" class="btn btn-outline-secondary btn-sm">Cancel</a>
    </div>
</div>
